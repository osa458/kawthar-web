import type { FieldHook } from '../../config/types.js';
type HookArgs = {
    /**
     * Current field name for the slug. Defaults to `slug`.
     */
    fieldName?: string;
    fieldToUse: string;
};
/**
 * This is a `BeforeChange` field hook used to auto-generate the `slug` field.
 * See `slugField` for more details.
 */
export declare const generateSlug: ({ fieldName, fieldToUse }: HookArgs) => FieldHook;
export {};
//# sourceMappingURL=generateSlug.d.ts.map